services:
  - type: web
    name: capstone2-app
    runtime: docker
    plan: free
    dockerContext: "."
    dockerfilePath: "Capstone 2/Dockerfile"
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: "10000"
